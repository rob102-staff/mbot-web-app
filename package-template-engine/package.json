{
  "name": "mbot-package-template-engine",
  "version": "1.0.1",
  "description": "A tool that is meant to be run using npx that can generate mbot package templates",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Franklin Volcic",
  "license": "ISC",
  "bin": {
    "mbot-package-template-engine": "bin/index.js"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "git-clone": "^0.2.0"
  }
}
